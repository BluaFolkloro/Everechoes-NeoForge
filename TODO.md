# Everechoes 待办清单

更新时间：2026-09-19

当前目标：先完成一个可验证的最小邮件闭环——玩家创建草稿、编辑内容、设置地址、封蜡、拆封，并保证数据可保存和同步。

## P0：解除开发阻塞

- [x] 安装 JDK 21，并配置 `JAVA_HOME` 与 `PATH`
  - 已完成：Temurin 21.0.12.1；本地完整构建已通过。
  - 验收：`java -version` 显示 Java 21。
  - 验收：`.\gradlew.bat build` 成功完成。
- [x] 确认 GitHub Actions 的 `Build` 工作流在 `main` 分支通过
  - 已确认：提交 `4620763` 的 Build #38 成功完成。
  - 验收：最新提交的构建检查为绿色。
- [x] 清理或改造 `Config.java` 中的 NeoForge MDK 示例配置
  - 已完成：移除未使用的示例配置类及注册代码。
  - 验收：不再生成与项目无关的 dirt block、magic number、示例物品配置。

## P0：完成信件数据层接入

- [x] 决定 `LetterData` 的组件值策略
  - 已改为不可变值对象；编辑和状态转换都返回新的 `LetterData`。
  - 原因：Minecraft Data Component 不适合直接保存可变共享对象。
- [x] 新建并注册信件 Data Component
  - 使用 `LetterDataSerializer.CODEC` 作为持久化编解码器。
  - 使用 `LetterDataSerializer.STREAM_CODEC` 作为网络同步编解码器。
  - 在模组入口注册组件类型。
- [x] 将信件组件挂接到 `letter`、`sealed_letter`、`opened_letter`
  - 新建信件时自动生成 `letterId` 和退回地址。
  - 三种物品转换时完整保留同一份信件数据与 `letterId`。
- [x] 为 `AddressSerializer` 和 `LetterDataSerializer` 增加往返测试
  - 覆盖 `MailBoxAddress` 与 `PlayerAddress`。
  - 覆盖 `DRAFT`、`SEALED`、`OPENED`。
  - 覆盖缺失收件地址、未知状态、空值与畸形数据。
- [x] 为 `LetterData` 状态机增加单元测试
  - 验收：只允许 `DRAFT -> SEALED -> OPENED`。
  - 验收：封蜡后正文与地址均不可编辑。
  - 验收：蜡封和拆封状态必须存在收件地址。

## P1：完成信件交互闭环

- [x] 实现草稿信件编辑界面
  - 字段：标题、正文、信内称呼、落款、收件地址。
  - 限制正文长度、标题长度，并在服务端再次校验。
- [x] 实现编辑数据的客户端/服务端同步
  - 使用自定义 payload 或合适的菜单数据通道。
  - 服务端验证物品仍在玩家手中且仍为草稿。
- [x] 实现封蜡动作
  - 缺少收件地址时显示已有的本地化提示。
  - 成功后将 `letter` 转换为 `sealed_letter`，保留组件数据。
- [x] 实现拆封动作
  - 将 `sealed_letter` 转换为 `opened_letter`。
  - 已拆封信件不可再次封蜡或编辑。
- [x] 实现已封蜡/已拆封信件的只读查看界面
- [x] 增加物品提示信息
  - 显示状态、收件对象、标题摘要等非敏感信息。

## P1：建立三级邮政地址与权限模型

- [x] 定义“邮域—邮区—邮编”的稳定标识和值对象
  - 完整邮箱地址必须同时包含 `domainId`、`districtId` 和 `postalCode`。
- [x] 设计服务器级邮域注册表
  - 邮域无主，内部 UUID 与显示代码分离，存在 overworld 的 SavedData 中。
  - 开发版不保证旧邮域绑定兼容；升级后邮筒可能需要重新入网。
- [x] 让 `post_box` 作为邮区节点申请加入或退出邮域
  - GUI 只提交请求；服务端按策略处理 PENDING/ACTIVE/LEAVING/DETACHED。
- [x] 建立邮区辖区服务端模型
  - 邮区辖区由同一维度内连续且已探索的区块组成；不同邮区可以重叠。
  - 邮筒必须位于本邮区辖区；邮域范围由有效下辖邮区自动派生。
  - 记录玩家进入过的区块，不为探索或查询强制加载区块。
- [x] 实现邮政地图册与辖区编辑界面
  - 地图册只是辖区变更界面，不是玩家所有权或永久管理凭证。
  - MVP：手持 `postal_atlas` 对已入邮区的邮筒使用；21×21 区块网格；重叠为合法标记；提交走 `replaceDistrictCoverage` 与 revision 校验。
  - 第一版不是地形地图；正式 GUI 贴图仍待预览确认后进包。
- [ ] 实现通用邮区代理中转协议
  - 所有邮区均可申请指定网关邮区作为代理，不依赖飞地判断。
  - 代理不改变邮区归属、地址或邮域派生范围，并必须防止循环路由。
- [ ] 让 `mail_box` 创建具体邮编端点
- [ ] 将邮编申领权与邮箱方块绑定关系分离
  - 玩家可以保留喜欢的邮编，但未绑定邮箱的邮编不是有效投递端点。
  - 不创建虚假邮箱、占位库存或幽灵投递队列。
- [ ] 设计邮箱钥匙 Data Component 与服务端权限校验
  - 玩家不直接拥有邮箱；钥匙授予访问权限。
  - 支持复制钥匙、重新配锁和旧钥匙失效。

## P1：把发信邮筒改造成承运交接入口

- [x] 明确发信邮筒 GUI 的最小设计
  - 当前：5 格投递槽，只交接封蜡信件；承运方式暂为玩家领取。
- [x] 限制投递槽只接受带有效组件的 `sealed_letter`
- [x] 用正式投递菜单替换 27 格 `SimpleContainer` 占位实现
- [x] 把信件内容数据与运输数据分离
  - `LetterData` 保存正文和生命周期。
  - `Waybill` 保存运单号、待领取/承运中和承运玩家。
- [x] 实现玩家从 `post_box` 领取并手动承运邮件
  - 玩家承运不限制距离或维度。
- [ ] 实现玩家、信鸽、邮筒和邮箱之间的服务端交接校验
- [x] 保证上/下半邮筒打开、破坏与掉落行为在重构后仍正确

## P2：实现收信、签收、退回与中转

- [ ] 新增 `mail_box` 收信邮箱方块、方块实体与 GUI
- [ ] 实现钥匙访问、收件查看和邮箱签收
- [ ] 实现 `PlayerAddress` 当面交付
  - 邮件不会自动进入背包，玩家必须与承运者交互签收。
- [ ] 允许玩家声明默认收件邮箱
  - 信鸽优先改投默认邮箱，不再寻找玩家实时位置。
  - 保留“必须本人签收”的信件选项。
- [ ] 实现失败后沿 `returnAddress` 实体退回
- [ ] 实现实际中转站点及滞留邮件库存
  - 滞留邮件必须存在于某个真实节点，不只是一条无主数据库记录。
- [ ] 定义邮箱被破坏、邮编迁移和重新绑定规则
  - 与邮编申领/自动分配 GUI 一起定案。

## P2：实现信鸽运输且不加载沿途区块

- [ ] 定义信鸽承运属性：速度、航程、导航、负重和返航能力
- [ ] 设计末影升级以提供跨维度能力
- [ ] 支持为信鸽登记传送门路线
- [ ] 实现“附近实体、远处逻辑旅行”的混合模型
  - 离开玩家活动区域后将信鸽和邮件转为持久化旅程记录。
  - 到达已加载的起点、终点或交互区域时再实体化。
- [ ] 禁止信鸽持续加载沿途区块
- [ ] 按 `nextEventTime` 调度到期旅程，不逐 tick 扫描所有信鸽
- [ ] 目的地区块未加载时保持逻辑抵达，不强制加载区块
- [ ] 为中断、失踪、超时、返航和中转失败制定状态转换

## P2：多人安全与可靠性

- [ ] 所有编辑、封蜡、拆封、投递操作进行服务端权限校验
- [ ] 防止复制、重放和同一 `letterId` 重复投递
- [ ] 为超长正文和恶意网络数据设置硬限制
- [ ] 保证每封在途邮件都有明确承运者或实际存放节点
- [ ] 区分承运旅程记录与自动投递队列，禁止无承运者的后台搬运
- [ ] 增加失败日志，但避免在日志中记录完整私人信件正文
- [ ] 增加多人集成测试或 GameTest

## P3：内容与体验完善

- [ ] 为信件、邮筒和三个夜鹭手办增加合成配方
- [ ] 为三个夜鹭手办补充方块掉落表
- [ ] 制作正式的邮筒、写信和阅读 GUI 资源
- [ ] 增加封蜡、投递、收信和拆信的音效/粒子/提示
- [ ] 实现不依赖邮费的邮票收藏系统
  - 支持邮域发行、节日系列和纪念邮票。
  - 支持出发戳、中转戳、到达戳和实寄封收藏。
  - 邮票不是投递门槛，也不引入强制经济消耗。
- [ ] 增加数据生成器，减少手写模型、语言和掉落表的重复工作
- [x] 更新 README，使其与已实现的 `LetterData` 和序列化器保持一致
- [ ] 补充用户安装说明、服务端说明与开发者贡献说明
- [ ] 将版本从 `0.0.1` 提升到首个可玩里程碑版本

## 建议里程碑

### M0：可构建骨架

JDK 21 配置完成，本地与 CI 构建通过，模板配置清理完毕。

### M1：本地信件闭环

已完成。玩家可以写信、封蜡、拆封；数据可随物品保存并正确同步，但暂不支持邮筒投递。

### M2：人工邮路闭环

进行中。无主邮域模型、邮筒玩家承运和邮政图册辖区编辑 MVP 已可用。邮箱钥匙、`mail_box` 签收和当面交付尚未实现。

### M3：信鸽邮路闭环

信鸽可以承运、使用升级或登记传送门跨维度，并在不加载沿途区块的情况下完成逻辑旅行、签收、退回和中转。

### M4：多人可靠版本

支持服务器持久化、权限校验、异常恢复、大量信鸽调度和基础测试。
